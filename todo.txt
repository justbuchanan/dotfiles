add lightdm-gtk-greeter so enabling of lightdm service doesnt fail



npm install -g i3-style
# Usage: i3-style solarized -o ~/.config/i3/config --reload

remove trackpad stuff from .xinitrc, it's laptop-specific


# automate installation of sublime package control

auto-detect monitors?

put arandr in quick-launch menu

install rsync

set GTK_THEME=Adapta variable
